[project]
name = "wechat-extract-mac"
version = "0.1.0"
description = "WeChat Mac Chat History Exporter"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "flask>=2.3.0",
    "requests>=2.31.0",
    "zstandard>=0.21.0",
]

[tool.uv]
package = false
